const Flow =(props) =>{

    return(
        <>
        <div className="prop">
        <h1>Name = {props.name}</h1>
        <h2>Age = {props.age}</h2>
        </div>
        </>
    )
} 

export default Flow